replicaCount: 1

image:
  repository: fm-mcp-filesystem
  tag: latest
  pullPolicy: Never

service:
  port: 3000

workspace:
  basePath: ""  # Set via Ansible (resolved to absolute path at deploy time)

namespace: forgemaster-system

resources:
  limits:
    cpu: 250m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
