{{- if .Values.controller.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: fm-controller-agenttask
  labels:
    app.kubernetes.io/name: fm-controller-agenttask
    app.kubernetes.io/component: controller
    app.kubernetes.io/part-of: forgemaster
rules:
  - apiGroups:
      - forgemaster.io
    resources:
      - agenttasks
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - forgemaster.io
    resources:
      - agenttasks/status
    verbs:
      - get
      - update
      - patch
  - apiGroups:
      - ""
    resources:
      - namespaces
    verbs:
      - get
      - list
      - watch
      - create
      - delete
  - apiGroups:
      - ""
    resources:
      - pods
      - configmaps
      - secrets
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: fm-controller-agenttask
  labels:
    app.kubernetes.io/name: fm-controller-agenttask
    app.kubernetes.io/component: controller
    app.kubernetes.io/part-of: forgemaster
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: fm-controller-agenttask
subjects:
  - kind: ServiceAccount
    name: fm-controller-agenttask
    namespace: {{ .Values.namespace }}
{{- end }}
