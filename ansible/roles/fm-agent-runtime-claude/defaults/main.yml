---
# Image name (environment provides image_tag, image_registry)
fm_agent_runtime_claude_image_name: fm-agent-runtime-claude

# Build paths (environment provides build_enabled)
fm_agent_runtime_claude_dockerfile: "{{ playbook_dir }}/../crates/fm-agent-runtime-claude/Dockerfile"
fm_agent_runtime_claude_build_context: "{{ playbook_dir }}/.."
